extends ../../layouts/default
include ../../mixins/display-error
block content
	.container
		h1 Error (500)
		p.lead Sorry, the site has encountered an error.
		.content
			if(error)
				+displayError(error)